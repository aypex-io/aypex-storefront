<div class="taxon">
  <div class="container">
    <%= aypex_breadcrumbs(@taxon) %>
    <%= render 'header', taxon: @taxon %>
  </div>
  <%= render 'subcategories', taxon: @taxon %>
  <div data-hook="taxon_products">
    <%= render template: 'aypex/products/index', colors: @colors, sizes: @sizes %>
  </div>
</div>
